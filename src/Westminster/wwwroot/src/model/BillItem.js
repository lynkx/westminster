export class BillItem {
    constructor(orderNumber, amount, products) {
        this.orderNumber = orderNumber;
        this.amount = amount;
        this.products = products == null ? products : [];
    }
}
//# sourceMappingURL=BillItem.js.map